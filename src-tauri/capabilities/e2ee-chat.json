{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "e2ee-chat",
  "description": "MLS/openmls permissions for the E2EE chat client. Not granted on iOS: the chat client and its plugin are excluded from the iOS build (pure instance client for now), and referencing a permission of an absent plugin would fail the build.",
  "platforms": ["macOS", "windows", "linux", "android"],
  "windows": [
    "main",
    "secure-chat",
    "main-window",
    "chat-window",
    "unified",
    "main-webview",
    "chat-webview",
    "chrome-bar",
    "split-divider"
  ],
  "permissions": [
    "openmls:default"
  ]
}
