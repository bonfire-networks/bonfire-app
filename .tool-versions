# You can use asdf or [mise](https://mise.jdx.dev/) to install these
#### alpine    3.23.5
# ^ NOTE: the alpine version must match the one used by the elixir version in use: https://hub.docker.com/r/hexpm/elixir/tags?name=alpine
# erlang    25.3.2.8
# erlang    27.3.4.1
# erlang    28.5.0.1
erlang      29.0.6
# elixir       1.13.4-otp-25
# elixir    1.18.4-otp-27
# elixir    1.19.5-otp-28
elixir    1.20.4-otp-29 
####
# yarn      1.22.22
# NOTE: pin node rather than tracking `latest`, to match the `node:24-alpine` stage in Dockerfile.release and to keep bare-metal release builds off prerelease node builds, which yarn 1 rejects (a prerelease version never satisfies a plain semver range like esbuild's `>=12`)
nodejs    26.8.1
# chromedriver latest
# ^ we don't want this in CI
# gh        latest
just      latest
transifex latest
# dexter  latest
